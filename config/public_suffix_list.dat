# This project uses tldextract's bundled Public Suffix List snapshot at runtime
# with suffix_list_urls=() and cache_dir=None to prevent live network fetches.
# Keep this file as the repository marker for the offline PSL requirement.
